data_config: 'mode_1'
model: 'CMamba_v2'

name: 'CMamba'
max_epochs: 1000
use_volume: False
additional_features: ["vol_current","bitbo_sharpe_ratio","day_of_year","vol_weekend"]

skip_revin: []

hyperparams:
    optimizer: 'adamw'
    lr: 0.0001
    weight_decay: 0.001
    loss_type: 'mse'
    alpha: 0.7
